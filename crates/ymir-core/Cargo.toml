[package]
name = "ymir-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Core types, traits, and provenance tracking for the Ymir pipeline"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
rand_pcg = "0.3"
